spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/pethelpbd?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: admin
  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.r2dbc: DEBUG
    com.perros.registro_perros: DEBUG
    org.springframework.web: DEBUG

server:
  port: 8080
